## Purpose

This widget is used to display the subathon clock. [Once a subathon is started](/features/subathons#start-a-subathon) from the dashboard, the clock will automatically start counting down. When the subathon ends, the clock will show `[ENDED]` instead.

## Preview